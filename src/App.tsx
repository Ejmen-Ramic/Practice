import MainContainer from "./components/MainContainer";

const App: React.FC = () => {
  return <MainContainer />;
};

export default App;

/**
 * Anything inside tags is text or component, put it in brackets to make it work
 * Put it above render
 */

/**
 * No need to render
 *
 *
 */
